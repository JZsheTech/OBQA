当前系统的LLM在回答时的输出仍然不够规范，为了让前端展示更加稳定，我需要你检查answerAgent的prompt，并补充上必要的输出格式要求：

格式要求：
- 对元素的引用格式(严格要求)：
1次引用多个元素： `[Elem#id_1, Elem#id_2]` ，即使elem_id是连续的，也不能写成`[Elem#id_0-id_3]`
1次引用单个元素： `[Elem#id]`
- 对排版格式的要求：
	- 使用markdown格式
	- 数学公式用`$`包裹行内公式，用`$$`包裹行间公式。
