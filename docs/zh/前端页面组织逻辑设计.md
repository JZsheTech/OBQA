# 页面组织和逻辑宏观设计

## 顶部标签栏
知识库主页、chat历史

## 顶部导航栏
根据当前所处的页面位置不同存在下面几种状态：

home 位于主页
collection名 位于某个collection的管理页
collection名 > chat名 位于针对某个collection的某个chat中

collection名 > 文档名 位于某个document的管理页
collection名 > 文档名 > chat名 位于针对某个document的某个chat中

## 页面1-知识库主页

要展示的内容和组件
- collection列表，点击其中的一个条目将跳转到对应Collection的文档管理页面
	- 列表的条目要展示下面的信息: collection name、 created_at时间、 description描述(截取前面部分显示)
- 新建collection的 "+" 按钮
- 新建collection的弹出组件： 要求输入collection name 和 description描述
- collection搜索组件： 按关键词搜索collection name 或者 description的搜索框(搜索后collection列表中只显示过滤后符合条件的collection，并变换一个颜色，加一个小提示searched result，允许选择按 name搜索还是按description搜索)
- reset按钮 ： 重置搜索视图和搜索框。

## 页面2-collection页

要展示的内容和组件：
- collection的完整信息
	- collection name
	- collection description
	- collection created date

- 文档document列表组件
	- 显示当前collection中的所有文档的列表，仅包括title和file_name信息
	- 点击其中的1个条目将跳转至对应的document页。
- 上传文档的button "+"按钮 (点击后允许用户选择多个本地文件上传到指定的collection)
- 搜索框：按关键词搜索/全文检索：document tile/abstract/md_text 的搜索框,按全文检索相似度从高到低返回topK。 (允许选择按tile还是abstract还是按md_text(文档全文)搜索)
- 搜索视图：只展示符合搜索条件的document组成的列表，默认为空
	- 列表中的每个条目包括该document的title和file_name信息
	- 点击其中的1个条目将跳转至对应的document页。
- reset按钮 ： 重置搜索视图和搜索框。

- collection聊天历史组件
	- 以列表的形式展示针对当前collection的所有chat条目。
	- 每个chat条目都展示一下chat name。

简单collection-RAG搜索框和列表：
- 允许用户给关键词或者短句，调用后端的RAG方法，将检索到的element的text_content直接以列表的形式展现出来。点击某个text_content后可弹出完整的片段组件，并允许复制，列表中显示时采取截断显示的方法。

## 页面3 document页

要展示的内容和组件：

当前document的详细信息
- 所属的collection name
- title
- file_name
- num_pages
- element_count

abstract文本块展示
- abstract

- document聊天历史组件:
	- 以列表的形式展示针对当前document的所有chat条目。
	- 每个chat条目都展示一下chat name。

简单document-RAG搜索框和列表：
- 允许用户给关键词或者短句，调用后端的RAG方法，将检索到的element的text_content直接以列表的形式展现出来，点击某个text_content后可弹出完整的片段组件，并允许复制，列表中显示时采取截断显示的方法。


## 页面4-问答页(针对单个collection)
左侧展示用户问题 和 系统回答，类似于chatgpt的对话形式
右侧展示预览的pdf，并给出一个下拉框，用于选择该collection中的不同document，其中显示document的title。
当用户点击系统回答中出现的evidence锚点时，右侧pdf预览界面能直接跳转到对应的文档的对应页面并高亮相关element

参考界面如下：
![[Pasted image 20251120210220.png]]

侧边栏：
- collection聊天历史组件，作为一个可以折叠的侧边栏出现，可以复用collection页面出现的那个。

## 页面5-问答页(针对单个document)
左侧展示用户问题 和 系统回答，类似于chatgpt的对话形式
右侧展示预览的pdf
当用户点击系统回答中出现的evidence锚点时，右侧pdf预览界面能直接跳转到对应的页面并高亮相关element

![[Pasted image 20251120210529.png]]

![[Pasted image 20251120210606.png]]

侧边栏：
- document聊天历史组件，作为一个可以折叠的侧边栏出现，可以复用document页面出现的那个。

## 页面6-chat-history页面

- collection level的chat history列表
	- 每个条目前注明collection_name，然后跟上chat_name，点击后可跳转到对应的collection问答页

- document level的chat history列表
	- 每个条目前注明collection_name > document title, 然后跟上chat_name，点击后可跳转到对应的document问答页

列表中条目顺序按照chat的创建时间(created_at)排序。

# **全局风格 & 组件库（中文）**

> **全局风格 & 组件库**
>
> 为一个 “基于证据的论文问答系统（Evidence-based Paper QA Agent）” 创建桌面 Web App 的设计系统。
> 画布尺寸 1440×900，浅色主题，风格简洁现代，以中性灰和蓝色点缀为主。
>
> 包含以下内容：
>
> * 顶部应用栏：左侧放产品 Logo，右侧用户头像。
> * 二级顶部标签栏：包含 “知识库主页” 与 “Chat 历史” 两个标签。
> * 面包屑导航模式（例如：“Home / Collection 名 / 文档名 / Chat 名”）。
> * 常见布局模式：
>
>   * 双栏布局（左列表 + 右详情）
>   * 三栏布局（左 Chat、中间 PDF Viewer、右侧 Sidebar）
> * 可复用 UI 组件，全部使用 Auto Layout，层级命名清晰，便于后续代码导出：
>
>   * Button / IconButton / SegmentedControl / Tag / Badge / Chip
>   * SearchBar（带筛选下拉菜单 + Reset 清空按钮）
>   * Collection / Document / Chat 用的 Card
>   * 用于 Collection / Document / Chat 历史的 List Item
>   * 分页组件 & 空状态提示组件
> * 创建一个 Token-based 的样式系统：
>
>   * 文本样式：页面标题、Section 标题、列表项标题、元数据标签等。
>     * 颜色样式：Primary、Secondary、Background、Surface、Border，以及用于 “搜索结果高亮” 和 “证据高亮” 的浅色。
>
> 全局使用 Auto Layout、8px 间距网格，桌面优先，同时保持响应式友好。

# 分页面详细设计

## **页面 1：知识库主页（Collections Home）**

> **页面 1：知识库主页（Collections Home）**
>
> 使用已有设计系统，为 Evidence-based Paper QA Web App 设计 “知识库主页 / Collections Home” 页面。
> 画布 1440×900，桌面端。
>
> ### 顶部区域：
>
> * 应用栏 + 顶部标签栏（“知识库主页” 选中）
> * 面包屑仅显示 “Home”
>
> ### 主体内容：
>
> * 顶部 Header 行：
>
>   * 左侧：页面标题 “知识库主页”
>   * 右侧：IconButton “+ 新建 Collection”
> * 工具栏行：
>
>   * 搜索框（placeholder：“按 name 或 description 搜索 Collection”）
>   * 下拉： “按 name 搜索 / 按 description 搜索”
>   * 只在过滤时显示的 “Searched result” 标签
>     * “Reset” 次要按钮
>
> ### 主列表：
>
> * 使用竖直排列的卡片或列表行展示 Collection
> * 每个条目包含：
>
>   * Collection name（主标题）
>   * created_at（灰色小字）
>   * description 的前部分（截断显示）
> * Hover & 点击状态：点击跳转到对应的 Collection 管理页面
>
> ### 新建 Collection 的 Modal：
>
> * 居中弹窗，标题 “新建 Collection”
> * 两个输入框：“Collection name”、“Description”
> * Primary 按钮 “创建”，Secondary 按钮 “取消”
>
> 使用 Auto Layout，主要层命名：`TopTabs`, `Breadcrumb`, `CollectionList`, `CollectionCard`, `NewCollectionModal`

---

## **页面 2：Collection 管理页**

> **设计 “Collection 管理页 / Collection Detail” 页面**，使用现有设计系统。
> 画布 1440×900。
>
> ### 顶部
>
> * 应用栏 + 顶部标签栏（知识库主页可见但未选中）
> * 面包屑：`Collection 名`
>
> ### 页面头部区域
>
> * 大标题显示 Collection 名
> * 下方显示完整描述 + 创建时间（例如：“创建时间：YYYY-MM-DD HH:MM”）
>
> ### 主布局：双列布局
>
> * 左列：文档列表（Documents）
> * 右列：Collection Chat 历史 + 简单的 Collection 级 RAG 检索
>
> ---
>
> ## **左列 – 文档列表**
>
> * Section Header： “文档列表” + IconButton “+ 上传文档”
> * 搜索栏 + 下拉选择搜索范围：“title / abstract / md_text(全文)”
> * Toggle 或 Tabs：“全部文档 / 搜索结果”
> * Reset 按钮
>
> **文档列表：**
>
> * 展示该 Collection 下的所有文档（列表行）
> * 每行显示：
>
>   * `title`（主标题）
>   * `file_name`（副标题）
> * 点击进入 Document 管理页
> * 搜索结果需用浅色背景或 Tag 显示 “Searched result”
>
> ---
>
> ## **右列 – Collection Chat 历史 + 简单 RAG**
>
> ### Collection 聊天历史
>
> * Section 标题：“Collection 聊天历史”
> * 列表展示 chat 名称
> * 点击跳转到 Collection Chat 问答页
>
> ### 简单 Collection-RAG 检索
>
> * 输入框（单行或多行均可）
> * Primary “检索” 按钮
> * 下方展示匹配 element 的 `text_content`（截断的内容）：
>
>   * 行或卡片形式
>   * 每条右上展示 “Evidence” tag
>   * 点击可弹出完整内容的浮层，带 “复制文本” 按钮
>
> 使用层命名：`CollectionInfo`, `DocumentSearchBar`, `DocumentList`, `CollectionChatHistory`, `CollectionRAGList`, `RAGSnippetDialog`

---

## **页面 3：Document 页**

> **设计 “Document 管理页 / Document Detail” 页面**
> 画布 1440×900。
>
> ### 顶部
>
> * 应用栏 + 顶部标签栏
> * 面包屑：`Collection 名 > 文档名`
>
> ### 文档信息区（顶部）
>
> * 显示所属 Collection（可点击返回）、title、file_name
> * 用 Chips 显示 meta 信息：`num_pages`, `element_count`
>
> ### 双列布局
>
> * 左列：Abstract + Document RAG 搜索
> * 右列：Document Chat 历史
>
> ---
>
> ## 左列
>
> ### Abstract
>
> * 使用 Card 展示 abstract（可滚动）
>
> ### 简单 Document-RAG 搜索
>
> * 输入框 + “检索”
> * 展示命中的 element 文本（截断）
> * 点击条目 → 弹出完整片段 + “复制文本”
>
> ---
>
> ## 右列
>
> ### Document 聊天历史
>
> * 显示该文档下的所有 chats（只显示 chat name）
> * 点击跳转到 Document Chat 问答页
>
> 层命名：`DocumentInfo`, `AbstractCard`, `DocumentRAGList`, `DocumentChatHistory`

---

## **页面 4：Collection 问答页（Chat + 多文档 PDF）**

> **设计 “Collection Chat with Evidence” 页面**
> 三栏布局：左 chat，中间 PDF Viewer，右 sidebar
> 画布 1440×900
>
> ### 顶部
>
> * 应用栏 + 顶部标签栏
> * 面包屑：`Collection 名 > Chat 名`
>
> ---
>
> ## 左侧 Chat 面板（40%）
>
> * Header：“针对 Collection 的问答”
> * ChatGPT 风格对话列表（用户泡泡 / 系统泡泡）
> * 底部：多行输入框 + “发送” 按钮 + 提示文字
> * 系统回答中包含可点击的 `[Evidence#1]` / `[Evidence#2]` 标签（Chip）
>
> ---
>
> ## 中间 PDF Viewer（40%）
>
> * 顶部 Dropdown：选择本 Collection 的文档（用 title）
> * IconButton：打开原始 PDF
> * 主区域：PDF 页视图（可滚动）
> * 用半透明矩形表示 evidence 的 bounding box 高亮
>
> ---
>
> ## 右侧 Sidebar（可折叠，20%）
>
> * 标题：“Collection 聊天历史”
> * 展示 chat 列表，当前 chat 高亮
>
> ---
>
> ## 交互要求：
>
> * 点击 Evidence Tag → PDF viewer 自动跳页并高亮对应 element
> * 高亮用浅色、柔和的颜色
>
> 层命名：`ChatPanel`, `MessageList`, `EvidenceTag`, `PdfViewer`, `DocSelector`, `CollectionChatSidebar`

---

## **页面 5：Document 问答页（Chat + 单文档 PDF）**

> **设计 “Document Chat with Evidence” 页面**
> 与 Collection Chat 类似，但文档固定为单个。
> 画布 1440×900
>
> ### 顶部
>
> * 应用栏 + 顶部标签栏
> * 面包屑：`Collection 名 > 文档名 > Chat 名`
>
> ---
>
> ## 主布局（两栏 + Sidebar）
>
> ### 左栏：Chat Panel
>
> * 与 Collection Chat UI 一致
> * 回答中展示 `[Evidence#1]` 可点击 tag
>
> ### 右栏：PDF Viewer
>
> * 展示当前文档的 PDF 预览（无文档选择下拉）
> * 支持 evidence 高亮占位
>
> ### Sidebar（可折叠）
>
> * 标题：“Document 聊天历史”
> * 列表显示所有 document-level chats，当前 chat 高亮
>
> 层命名：`DocumentChatPanel`, `DocumentPdfViewer`, `EvidenceHighlight`, `DocumentChatSidebar`

---

## **页面 6：Chat 历史页**

> **设计 “Chat 历史 / Chat History” 页面**
> 画布 1440×900
>
> ### 顶部
>
> * 应用栏 + 顶部标签栏（“Chat 历史” 选中）
> * 面包屑：`Chat 历史`
>
> ### 页面头部
>
> * 标题 “Chat 历史”
> * 可选过滤控件：下拉选择 “全部 / Collection chat / Document chat”
>
> ---
>
> ## 主内容区域：两个 Section
>
> ### 1. Collection 级别 Chat 历史
>
> * Section 标题 “Collection 级别 Chat”
> * 按创建时间降序排序
> * 条目展示：
>
>   * `collection_name` + “ · ” + `chat_name`
>   * 创建时间（小字）
> * 点击跳转到对应的 Collection Chat 页
>
> ---
>
> ### 2. Document 级别 Chat 历史
>
> * Section 标题 “Document 级别 Chat”
> * 按 created_at 降序
> * 条目展示：
>
>   * `collection_name > document_title` + “ · ” + `chat_name`
>   * 创建时间
> * 点击跳转到 Document Chat 页
>
> 支持滚动与空状态。
>
> 层命名：`ChatHistoryPage`, `CollectionChatHistoryList`, `DocumentChatHistoryList`, `ChatHistoryItem`

---




