# 宏观交互逻辑设计（已合并）

本文件内容已合并入主设计文档：`docs/zh/多模态论文问答系统设计文档.md`。

- 接口路径统一为以 `/api` 为前缀（`/healthz` 例外）。
- 响应 envelope 统一为成功 `{"code":"OK","data":...}`，错误 `{"code":"SOME_ERROR","message":"..."}`。
- 交互流程采用同步设计，已移除后台异步与轮询说明。

请以主设计文档为准，后续更新不再在本文件重复维护。

